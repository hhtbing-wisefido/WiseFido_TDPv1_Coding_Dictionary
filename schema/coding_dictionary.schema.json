{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$comment": "VS Code JSON Schema Validator compatible version - v2.0.0 精简版",
  "title": "CodingItem",
  "description": "符合FHIR标准的编码条目 (v2.0.0)。当前版本只保留核心字段，未来可按需增加category、synonyms等扩展字段。",
  "type": "object",
  "required": ["system", "code", "display", "display_zh"],
  "properties": {
    "system": {
      "type": "string",
      "description": "编码系统 URI (如 http://snomed.info/sct)",
      "pattern": "^https?://.+",
      "minLength": 10
    },
    "code": {
      "type": "string",
      "description": "编码值 (如 217082002)",
      "minLength": 1,
      "maxLength": 50
    },
    "display": {
      "type": "string",
      "description": "英文显示名称 (如 Accidental fall)",
      "minLength": 1,
      "maxLength": 200
    },
    "display_zh": {
      "type": "string",
      "description": "中文显示名称 (如 意外跌倒)",
      "minLength": 1,
      "maxLength": 100
    }
  },
  "additionalProperties": true,
  "_note": "additionalProperties设为true以支持未来按需扩展字段，如category、synonyms、description等"
}
